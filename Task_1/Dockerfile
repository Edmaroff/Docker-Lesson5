# Используем последнюю версию nginx в качестве базового образа
FROM nginx

# Копируем файлы в директорию по умолчанию для nginx
COPY ./html /usr/share/nginx/html

# Контейнер слушает порт 80 после запуска
EXPOSE 80